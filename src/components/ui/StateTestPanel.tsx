case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }@case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Qcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }@case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }qcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }*case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }*case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }*case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }—case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }*case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }*case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }3case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }3case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }*case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }qcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Qcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }!case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }!case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }qcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Qcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ócase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }[case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }_case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }_case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }_case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }_case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }]case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }5case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }%case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }5case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }%case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }zcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }5case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }4case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }4case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }4case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }2case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }5case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }zcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }9case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }7case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }8case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }4case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }4case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }jcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }2case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }2case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }4case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }8case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }5case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }3case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }3case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }3case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }3case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }5case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }&case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }&case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }zcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }2case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }2case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }kcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }"case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }zcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }6case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ycase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }=case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }zcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }9case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }(case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }xcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }-case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }mcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }fcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }?case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }7case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }4case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }0case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ccase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }wcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }rcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }'case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }Hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }gcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }hcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }:case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }1case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      },case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }{case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }.case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ecase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }lcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }scase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }pcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }acase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }bcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ucase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }tcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ocase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }ncase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }<case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }/case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }dcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }icase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }vcase 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }>case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      } case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      })case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }}case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }
case 'error': {
        const store = useAppStore.getState()
        if (!store.selectedDeviceId && devices[0]) {
          store.selectDevice(devices[0].id)
        }
        requestAnimationFrame(() => {
          useAppStore.getState().setAppState('error-tracking', 'No pudimos conectar con el servidor de seguimiento.')
        })
        break
      }